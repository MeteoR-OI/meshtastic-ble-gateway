# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# Tests / coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# Données locales (V0.2 : SQLite de la sonde)
*.db
*.sqlite
*.sqlite3

# Outils
.mypy_cache/
.ruff_cache/

# Runbooks opérationnels MHA (RPi) — locaux, non versionnés (traçabilité côté ops)
deploy/runbook-*.md

# OS / éditeurs
.DS_Store
.idea/
.vscode/
# BMAD (le gros est dans ~/.gitignore global ; ici : settings.json = uniquement les hooks BMAD)
.bmad-loop/runs/
.bmad-loop/cache/
.claude/settings.json
